define(function (require) {
    describe('Timer', function () {
        var Timer = require('./timer');
        beforeEach(function () {
            // this.timer = Timer();
        });
        it('should when start', function () {

        });
    });
});
